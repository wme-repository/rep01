---
context: default
phase: release-handoff
task: 0
total_tasks: 0
status: paused
last_updated: 2026-04-08T19:50:40.8117040-03:00
---

# BLOCKING CONSTRAINTS - Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: remote-main-is-unrelated - `origin/main` belongs to another project (`seo-ranker`) and has no merge base with this repository - do not open or force a merge to `main`; continue on `origin/meta-ads-mcp-readonly` or move to a dedicated repository first.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Blind PR to unrelated main | Attempting to merge this code into `origin/main` would combine unrelated histories and the wrong product. | blocking | Verify remote history before any PR; if `origin/main` is unrelated, keep a dedicated branch or move repositories before merging. |
| Accidental publication of local config | `.claude/settings.local.json` is user-local and should not enter project commits. | advisory | Keep `.claude/settings.local.json` unstaged unless the user explicitly requests publishing it. |

<current_state>
The codebase is paused after local release preparation. Functional work is complete locally: hardening, tests, transport smoke checks, editable install validation, and live read-only Meta UAT all passed. The repository was pushed safely to `origin/meta-ads-mcp-readonly` because `origin/main` is an unrelated project.
</current_state>

<completed_work>

Completed Tasks:
- Task 1: Hardened read-only Meta MCP runtime, logging, retries, pagination, and allowlist enforcement - Done
- Task 2: Added automated tests, CI workflow, and read-only UAT runner - Done
- Task 3: Validated local packaging and pushed project commits to remote branch `origin/meta-ads-mcp-readonly` - Done
</completed_work>

<remaining_work>

- Check the GitHub Actions run triggered by the branch push
- Decide whether this code should stay in `rep01` on its own branch or move to a dedicated repository
- If CI passes and repository strategy is confirmed, close the milestone as v1-complete
</remaining_work>

<decisions_made>

- Chose strict read-only validation end-to-end because write scope is out of v1 and increases risk
- Chose lazy runtime bootstrap because import-time env validation was hurting testability and CLI behavior
- Chose branch publication instead of pushing to remote `main` because `origin/main` is an unrelated seo-ranker project
- Kept `.claude/settings.local.json` out of commits because it is local user configuration
</decisions_made>

<blockers>
- Remote strategy unresolved: `rep01/main` is another project, so merge/promotion strategy needs human confirmation
- GitHub CLI is unauthenticated in this shell, so Actions inspection must happen in the browser or after `gh auth login`
</blockers>

## Required Reading (in order)
1. `.planning/STATE.md` - current session state and next recommended action
1. `.planning/ROADMAP.md` - milestone status and remaining external gates
1. `README.md` - release readiness, runtime usage, and UAT summary
1. `CHANGELOG.md` - version `0.1.0` release summary
1. `.github/workflows/ci.yml` - exact CI matrix and test command

## Critical Anti-Patterns (do NOT repeat these)

- Trying to merge this branch into `origin/main` without inspecting remote history -> verify branch ancestry first and keep this project isolated from unrelated remote history

## Infrastructure State

- Local branch: `main`
- Remote tracking branch: `origin/meta-ads-mcp-readonly`
- Remote URL: `https://github.com/wme-repository/rep01.git`
- Suggested PR URL from push output: `https://github.com/wme-repository/rep01/pull/new/meta-ads-mcp-readonly`
- Local uncommitted file preserved on purpose: `.claude/settings.local.json`
- Background processes: none
- GitHub CLI auth in this shell: not logged in

<context>
The important point is that there is no technical reason to continue editing code locally right now. The risk moved from implementation to release operations. The code is already validated locally and against the real Meta API in read-only mode. The next agent should start with CI observation and repository strategy, not with more feature work.
</context>

<next_action>
Start with: open the GitHub repository page for branch `meta-ads-mcp-readonly`, inspect the Actions run triggered by the push, and only then decide whether to keep this project in `rep01` or move it to a dedicated repository before any PR or merge attempt.
</next_action>
